import { Stack } from "@mui/material";
import FormLogin from "../sections/loginSections";

const Login = () => {
  return (
    <Stack justifyContent="center" alignItems="center" height="100vh">
      <FormLogin />
    </Stack>
  );
};

export default Login;
